# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.so

# Virtual environments
venv/
.venv/
env/
.env/

# Environment variables
.env
.env.local

# Distribution / packaging
dist/
build/
*.egg-info/
*.egg

# Jupyter
.ipynb_checkpoints/

# Pytest / coverage
.pytest_cache/
.coverage
htmlcov/

# Playwright browser binaries
playwright-browsers/
ms-playwright/

# Data — raw datasets and intermediate files not tracked
# Datos brutos y archivos intermedios no rastreados
/data/raw/
/data/processed/wines_SPA_clean.csv
/data/processed/wines_SPA_model_ready.csv
/data/processed/wines_SPA_enriched_FINAL.csv
# wines_SPA_enriched.csv IS committed — the app requires it at runtime
# wines_SPA_enriched.csv SÍ se sube — la app lo necesita en ejecución

# Models — large model files not tracked
# Modelos — archivos de modelo grandes no rastreados
models/*.pkl
models/*.joblib

# Logs
*.log
logs/

# IDE
.vscode/
.idea/
*.code-workspace

# OS
.DS_Store
Thumbs.db
desktop.ini

# Secrets
*.pem
*.key

# Preprocessing-generated files (not needed for the web app)
# Archivos generados por el preprocesamiento (no necesarios para la app web)
preprocessing/*.csv
preprocessing/*.pkl

